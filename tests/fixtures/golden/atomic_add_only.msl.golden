#include <metal_stdlib>
using namespace metal;

kernel void vg_linear_compute(device uint* allocation_0 [[buffer(0)]], uint3 gid [[thread_position_in_grid]]) {
  if (any(gid != uint3(0))) return;
  atomic_fetch_add_explicit((device atomic<ulong>*)((device uchar*)allocation_0 + 0), (ulong)3UL, memory_order_relaxed);
}
